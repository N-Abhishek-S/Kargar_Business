export const MARQUEE_DEFAULTS = {
  // Speed configuration (seconds per full cycle)
  duration: 30, 
  // Gap between logos (px or token based)
  gap: 24, 
  // Dataset limiting
  maxVisibleLogos: 30,
  // Minimum logos required to enable marquee (otherwise fallback to static layout)
  minLogosForMarquee: 5,
};
